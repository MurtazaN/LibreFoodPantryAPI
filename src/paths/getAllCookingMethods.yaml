description: List Cooking Methods
responses:
  "200":
    description: OK
    content:
      application/json:
        schema:
          $ref: "../index.yaml#/components/schemas/cookingMethodArray" #schema does not exist yet
