description: List Cooking Tips
responses:
  "200":
    description: OK
    content:
      application/json:
        schema:
          $ref: "../index.yaml#/components/schemas/cookingTipsArray" #Schema does not exist yet
